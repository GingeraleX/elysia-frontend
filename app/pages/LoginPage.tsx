"use client";

import React from "react";
import { AuthModal } from "@/app/components/auth/AuthModal";

/**
 * LoginPage - Full page authentication modal
 * Accessed via ?page=login
 */
export default function LoginPage() {
  return <AuthModal />;
}

